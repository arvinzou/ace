<view class="index">
  <view class="index-hd">
  
  </view>
  <view class="index-bd">
    <view class="kind-list">
      <block wx:for-items="{{list}}" wx:key="{{item.id}}">
        <view class="kind-list-item">
          <view id="{{item.id}}" class="kind-list-item-hd {{item.open ? 'kind-list-item-hd-show' : ''}}" bindtap="kindToggle">
            <view class="kind-list-text">{{item.name}}</view>
            <image class="kind-list-img" src="resources/kind/{{item.icon}}"></image>
          </view>
          <view class="kind-list-item-bd {{item.open ? 'kind-list-item-bd-show' : ''}}">
            <view class="navigator-box {{item.open ? 'navigator-box-show' : ''}}">
              <block wx:for-items="{{item.list}}" wx:for-item="o" wx:key="*item">
                <navigator url="pages/{{o.id}}" class="navigator">
                  <view  class="navigator-text" style="flex-direction:column;">
                    <view>{{o.name}}</view>
                    <view style="font-size:12px">地址：{{o.addr}}</view>
                    <view style="font-size:12px">电话：{{o.tel}} 服务时间：{{o.serviceTimeStart}}-{{o.serviceTimeEnd}}</view>
                  </view>
                  <view class="navigator-arrow"></view>
                </navigator>
              </block>
            </view>
          </view>
        </view>
      </block>
    </view>
  </view>
</view>